#include <stdio.h>
#include "main.h"
/**
 * add - adds two numbers
 * @n: first parameter to be passed
 * @m: second parameter to be passed
 * Return: the sum
 */
int add(int n, int m)
{
	int first;
	int second;
	int sum;

	first = n;
	second = m;
	sum = first + second;
	return (sum);
}

#include "main.h"
#include <stdio.h>
/**
 * _memset - function that fills memory with a constant byte.
 * @s: parameter to a memory address
 * @c: constant byte
 * @n: the first n bytes of numbers
 * Return: a pointer tomemory address
 */
char *_memset(char *s, char b, unsigned int n)
